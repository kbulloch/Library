<html>
    <head>
        <title>About</title>
    </head>
    <body>
        <h1>About {{ book.getTitle }} :</h1>

        {% for author in authors %}
        Written by:
        <h2> {{ author.getName }}</h2>
        {% endfor %}

        <form action="/{{ client.getId }}/rent/{{ book.getId }}">
            <button type="submit">Rent it</button>
        </form>
    </body>
</html>
